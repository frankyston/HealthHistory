<%= form_with(model: exam, local: true) do |form| %>
  <% if exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
        <% exam.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :exam_date %>
    <%= form.datetime_select :exam_date %>
  </div>

  <div class="field">
    <%= form.label :exam_location %>
    <%= form.text_field :exam_location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
