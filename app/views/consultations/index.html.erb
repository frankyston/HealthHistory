<p id="notice"><%= notice %></p>

<h1>Consultas</h1>

<div class = "row">
  <div class="col-sm-6">
    <form method="get" class="busca">
      <label>Título:</label>
      <input type="text" class="form-control" name="title" value="<%= params[:title] %>" >
      <input type="submit" value="Buscar" class="btn btn-primary" style="margin-top:10px">
    </form>
  </div>
</div>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Name of professional</th>
      <th>Consultation date</th>
      <th>User</th>
      <th>Shared with</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @consultations.each do |consultation| %>
      <tr>
        <td><%= consultation.title %></td>
        <td><%= consultation.name_of_professional %></td>
        <td><%= consultation.consultation_date %></td>
        <td><%= consultation.user_id %></td>
        <td><%= consultation.shared_with %></td>
        <td><%= link_to 'Show', consultation %></td>
        <td><%= link_to 'Edit', edit_consultation_path(consultation) %></td>
        <td><%= link_to 'Destroy', consultation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Consultation', new_consultation_path %>
